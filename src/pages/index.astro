---
import Layout from '../layouts/Layout.astro'

import { certifications } from '../data/certifications.js'
import { education } from '../data/education.js'
import { experience } from '../data/experience.js'
import Contact from '../components/contact.astro'

import { projects } from '../data/projects.js'

const EMAIL = import.meta.env.EMAIL || 'email@example.com'
const DESCRIPTION_PHRASE =
    import.meta.env.DESCRIPTION_PHRASE || 'Full Stack Senior & Technical Lead con más de 8 años de experiencia.'
const PORTFOLIO_NAME = import.meta.env.PORTFOLIO_NAME || 'Pablo Martínez'
---

<Layout title="Portfolio - Desarrollador Front-End">
    <section class="min-h-screen flex items-center justify-center relative overflow-hidden pt-20">
        <div class="absolute inset-0 overflow-hidden pointer-events-none">
            <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-cyan-500/10 rounded-full blur-3xl animate-float"></div>
            <div
                class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl animate-float"
                style="animation-delay: 2s;">
            </div>
        </div>

        <div class="container mx-auto px-6 z-10">
            <div class="text-center">
                <h1
                    class="text-6xl md:text-8xl font-bold mb-6 bg-linear-to-r from-cyan-400 to-purple-500 bg-clip-text text-transparent">
                    Hola, soy {PORTFOLIO_NAME}!
                </h1>
                <p class="text-xl md:text-2xl text-gray-400 mb-8 max-w-2xl mx-auto">
                    {DESCRIPTION_PHRASE}
                </p>
                <div class="flex gap-4 justify-center flex-wrap">
                    <a
                        href="/proyectos"
                        class="px-8 py-4 bg-linear-to-r from-cyan-500 to-purple-600 rounded-full text-white font-semibold hover:shadow-lg hover:shadow-cyan-500/50 transition-all hover:scale-105">
                        Ver Proyectos
                    </a>
                    <a
                        href="#contact"
                        class="px-8 py-4 border-2 border-cyan-500 rounded-full text-cyan-400 font-semibold hover:bg-cyan-500/10 transition-all hover:scale-105">
                        Contactar
                    </a>
                </div>

                <div class="mt-16 animate-bounce">
                    <svg class="w-6 h-6 mx-auto text-cyan-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
                    </svg>
                </div>
            </div>
        </div>
    </section>

    <section id="about" class="py-20 relative">
        <div class="container mx-auto px-6">
            <div class="grid md:grid-cols-2 gap-12 items-center">
                <div class="order-2 md:order-1">
                    <h2
                        class="text-5xl font-bold mb-6 bg-linear-to-r from-cyan-400 to-purple-500 bg-clip-text text-transparent">
                        Sobre Mí
                    </h2>

                    <p class="text-gray-400 text-lg mb-6 leading-relaxed">
                        Soy Desarrollador Full Stack Senior & Technical Lead con más de 8 años de experiencia
                        construyendo plataformas SaaS, sistemas cloud y herramientas de automatización para distintos
                        sectores: turismo, e-commerce, inmobiliaria, investigación y gestión empresarial.
                    </p>
                    <p class="text-gray-400 text-lg mb-6 leading-relaxed">
                        He diseñado, desarrollado y desplegado productos completos de principio a fin, abarcando
                        arquitectura, backend, frontend, bases de datos, integración de APIs, cloud computing, pagos en
                        línea y automatización de procesos.
                    </p>
                    <p class="text-gray-400 text-lg mb-6 leading-relaxed">
                        Mi enfoque combina solidez técnica, eficiencia y visión de producto, garantizando soluciones
                        escalables, seguras y mantenibles.
                    </p>
                    <p class="text-gray-400 text-lg mb-6 leading-relaxed">
                        Me apasiona crear software que resuelva problemas reales y evolucione con el tiempo, priorizando
                        siempre la estabilidad en producción y la calidad del código.
                    </p>
                    <p class="text-gray-400 text-lg mb-6 leading-relaxed">
                        Poseo un nivel de inglés C1.1, lo que me permite colaborar con equipos internacionales,
                        documentar y comunicarme con fluidez en entornos técnicos.
                    </p>
                </div>
                <div class="order-1 md:order-2">
                    <div class="relative">
                        <div
                            class="absolute inset-0 bg-linear-to-r from-cyan-500 to-purple-600 rounded-2xl blur-xl opacity-50">
                        </div>
                        <img src="/img/profile.jpg" alt="Profile" class="relative rounded-2xl w-full shadow-2xl" />
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="py-12">
        <div class="container mx-auto px-6">
            <h2
                class="text-5xl font-bold mb-12 text-center bg-linear-to-r from-cyan-400 to-purple-500 bg-clip-text text-transparent">
                Proyectos destacados
            </h2>

            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
                {
                    projects
                        .filter((p) => p.featured)
                        .map((project) => (
                            <a
                                href={`/proyectos/${project.id}`}
                                class="group bg-gray-900/50 backdrop-blur-sm rounded-xl overflow-hidden border border-cyan-500/20 card-hover">
                                <div class="relative overflow-hidden">
                                    <img
                                        src={project.image}
                                        alt={project.title}
                                        class="w-full h-64 object-cover transition-transform duration-500 group-hover:scale-110"
                                    />
                                    <div class="absolute top-4 right-4 px-3 py-1 bg-cyan-500 text-white text-sm font-semibold rounded-full">
                                        {project.category}
                                    </div>
                                </div>

                                <div class="p-6">
                                    <h3 class="text-2xl font-bold text-white mb-3 group-hover:text-cyan-400 transition-colors">
                                        {project.title}
                                    </h3>
                                    <p class="text-gray-400 mb-4 line-clamp-2">{project.description}</p>

                                    <div class="flex flex-wrap gap-2 mb-4">
                                        {project.technologies.map((tech) => (
                                            <span class="px-3 py-1 bg-cyan-500/10 text-cyan-400 rounded-full text-sm">
                                                {tech}
                                            </span>
                                        ))}
                                    </div>

                                    <div class="flex items-center text-cyan-400 font-semibold group-hover:gap-3 gap-2 transition-all">
                                        Ver proyecto
                                        <svg
                                            class="w-5 h-5 transition-transform group-hover:translate-x-1"
                                            fill="none"
                                            stroke="currentColor"
                                            viewBox="0 0 24 24">
                                            <path
                                                stroke-linecap="round"
                                                stroke-linejoin="round"
                                                stroke-width="2"
                                                d="M17 8l4 4m0 0l-4 4m4-4H3"
                                            />
                                        </svg>
                                    </div>
                                </div>
                            </a>
                        ))
                }
            </div>

            <div class="flex gap-4 justify-center flex-wrap mt-5">
                <a
                    href="/proyectos"
                    class="px-8 py-4 bg-linear-to-r from-cyan-500 to-purple-600 rounded-full text-white font-semibold hover:shadow-lg hover:shadow-cyan-500/50 transition-all hover:scale-105">
                    Ver Proyectos
                </a>
            </div>
        </div>
    </section>

    <section id="experience" class="py-20 bg-gray-900/30">
        <div class="container mx-auto px-6">
            <h2
                class="text-5xl font-bold mb-12 text-center bg-linear-to-r from-cyan-400 to-purple-500 bg-clip-text text-transparent">
                Experiencia Profesional
            </h2>

            <div class="max-w-4xl mx-auto space-y-8">
                {
                    experience.map((job, index) => (
                        <div class="relative pl-8 border-l-2 border-cyan-500/50 hover:border-cyan-500 transition-colors">
                            <div class="absolute left-0 top-0 w-4 h-4 -translate-x-[9px] bg-cyan-500 rounded-full animate-glow" />
                            <div class="bg-gray-900/50 backdrop-blur-sm p-6 rounded-xl border border-cyan-500/20 card-hover">
                                <div class="flex flex-wrap justify-between items-start mb-4">
                                    <div>
                                        <h3 class="text-2xl font-bold text-white mb-1">{job.role}</h3>
                                        <p class="text-cyan-400 font-semibold">{job.company}</p>
                                    </div>
                                    <span class="text-gray-400 text-sm">{job.period}</span>
                                </div>
                                <p class="text-gray-400 mb-4">{job.description}</p>
                                <div class="flex flex-wrap gap-2">
                                    {job.technologies.map((tech) => (
                                        <span class="px-3 py-1 bg-cyan-500/20 text-cyan-400 rounded-full text-sm">
                                            {tech}
                                        </span>
                                    ))}
                                </div>
                            </div>
                        </div>
                    ))
                }
            </div>
        </div>
    </section>

    <section id="education" class="py-20">
        <div class="container mx-auto px-6">
            <h2
                class="text-5xl font-bold mb-12 text-center bg-linear-to-r from-cyan-400 to-purple-500 bg-clip-text text-transparent">
                Formación
            </h2>

            <div class="grid md:grid-cols-3 gap-6">
                {
                    education.map((edu) => (
                        <div class="bg-gray-900/50 backdrop-blur-sm p-6 rounded-xl border border-cyan-500/20 card-hover">
                            <div class="mb-4">
                                <div class="w-12 h-12 bg-linear-to-r from-cyan-500 to-purple-600 rounded-lg flex items-center justify-center mb-4">
                                    <svg
                                        class="w-6 h-6 text-white"
                                        fill="none"
                                        stroke="currentColor"
                                        viewBox="0 0 24 24">
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 14l9-5-9-5-9 5 9 5z"
                                        />
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 14l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14z"
                                        />
                                    </svg>
                                </div>
                                <h3 class="text-xl font-bold text-white mb-2">{edu.title}</h3>
                                <p class="text-cyan-400 font-semibold mb-1">{edu.institution}</p>
                                <p class="text-gray-500 text-sm mb-3">{edu.year}</p>
                            </div>
                        </div>
                    ))
                }
            </div>
        </div>
    </section>

    <section id="education" class="py-20">
        <div class="container mx-auto px-6">
            <h2
                class="text-5xl font-bold mb-12 text-center bg-linear-to-r from-cyan-400 to-purple-500 bg-clip-text text-transparent">
                Certificaciones
            </h2>

            <div class="grid md:grid-cols-3 gap-6">
                {
                    certifications.map((cert) => (
                        <div class="bg-gray-900/50 backdrop-blur-sm p-6 rounded-xl border border-cyan-500/20 card-hover">
                            <div class="mb-4">
                                <div class="w-12 h-12 bg-linear-to-r from-cyan-500 to-purple-600 rounded-lg flex items-center justify-center mb-4">
                                    <svg
                                        class="w-6 h-6 text-white"
                                        fill="none"
                                        stroke="currentColor"
                                        viewBox="0 0 24 24">
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 14l9-5-9-5-9 5 9 5z"
                                        />
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 14l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14z"
                                        />
                                    </svg>
                                </div>
                                <h3 class="text-xl font-bold text-white mb-2">{cert.title}</h3>
                                <p class="text-cyan-400 font-semibold mb-1">{cert.institution}</p>
                                <p class="text-gray-500 text-sm mb-3">{cert.year}</p>
                            </div>
                        </div>
                    ))
                }
            </div>
        </div>
    </section>

    <Contact />
</Layout>
